diff --git a/src/mesh/Router.cpp b/src/mesh/Router.cpp
index fef29388e..07fd2bc40 100644
--- a/src/mesh/Router.cpp
+++ b/src/mesh/Router.cpp
@@ -708,6 +708,15 @@ void Router::perhapsHandleReceived(meshtastic_MeshPacket *p)
         return;
     }
 
+    // Check rate limiting for decoded packets
+    if (p->which_payload_variant == meshtastic_MeshPacket_decoded_tag) {
+        if (rateLimiter.shouldRateLimit(p->from, p->decoded.portnum)) {
+            LOG_DEBUG("Rate limiting packet from 0x%x on port %d", p->from, p->decoded.portnum);
+            packetPool.release(p);
+            return;
+        }
+    }
+
     if (shouldFilterReceived(p)) {
         LOG_DEBUG("Incoming msg was filtered from 0x%x", p->from);
         packetPool.release(p);
diff --git a/src/mesh/Router.h b/src/mesh/Router.h
index 58ca50f3d..86182a158 100644
--- a/src/mesh/Router.h
+++ b/src/mesh/Router.h
@@ -7,6 +7,7 @@
 #include "PacketHistory.h"
 #include "PointerQueue.h"
 #include "RadioInterface.h"
+#include "RateLimiter.h"
 #include "concurrency/OSThread.h"
 
 /**
@@ -18,6 +19,9 @@ class Router : protected concurrency::OSThread, protected PacketHistory
     /// Packets which have just arrived from the radio, ready to be processed by this service and possibly
     /// forwarded to the phone.
     PointerQueue<meshtastic_MeshPacket> fromRadioQueue;
+    
+    /// Rate limiter for controlling packet frequency
+    RateLimiter rateLimiter;
 
   protected:
     RadioInterface *iface = NULL;
